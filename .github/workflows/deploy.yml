# name: Deploy

# on:
#   workflow_call:
#     inputs:
#       environment:
#         required: true
#         type: string
#       image_tag:
#         required: true
#         type: string
#       app_url:
#         required: true
#         type: string
#     secrets:
#       SSH_HOST:
#         required: true
#       SSH_USER:
#         required: true
#       SSH_PRIVATE_KEY:
#         required: true

# jobs:
#   deploy:
#     runs-on: ubuntu-latest
#     environment: ${{ inputs.environment }}
#     steps:
#       - name: Deploy
#         uses: appleboy/ssh-action@v1
#         with:
#           host: ${{ secrets.SSH_HOST }}
#           username: ${{ secrets.SSH_USER }}
#           key: ${{ secrets.SSH_PRIVATE_KEY }}
#           script_stop: true
#           script: |
#             docker pull ghcr.io/${{ github.repository }}-backend:${{ inputs.image_tag }}
#             docker pull ghcr.io/${{ github.repository }}-frontend:${{ inputs.image_tag }}
#             cd /opt/myapp
#             IMAGE_TAG=${{ inputs.image_tag }} docker compose up -d --remove-orphans
#             for i in $(seq 1 12); do
#               curl -sf ${{ inputs.app_url }}/healthz && break || sleep 5
#             done || exit 1
#             echo "${{ inputs.image_tag }}" > .previous-sha

#   rollback:
#     runs-on: ubuntu-latest
#     needs: deploy
#     if: failure()
#     steps:
#       - name: Rollback
#         uses: appleboy/ssh-action@v1
#         with:
#           host: ${{ secrets.SSH_HOST }}
#           username: ${{ secrets.SSH_USER }}
#           key: ${{ secrets.SSH_PRIVATE_KEY }}
#           script: |
#             PREV=$(cat /opt/myapp/.previous-sha)
#             echo "Rolling back to ${PREV}"
#             cd /opt/myapp
#             IMAGE_TAG=${PREV} docker compose up -d --remove-orphans
