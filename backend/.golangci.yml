version: "2"

run:
  timeout: 5m
  tests: true

linters:
  enable:
    - gofmt
    - govet
    - errcheck
    - staticcheck
    - unused
    - gosimple
    - ineffassign
    - typecheck
  settings:
    errcheck:
      check-blank: true

issues:
  max-issues-per-linter: 0
  max-same-issues: 0
