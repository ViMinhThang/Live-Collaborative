# name: Build and Push Images
# on:
#   push:
#     branches:
#       - main
#   pull_request:

# jobs:
#   lint:
#     runs-on: ubuntu-latest
#     steps:
#       - uses: actions/checkout@v4
#       - uses: actions/setup-go@v5
#         with:
#           go-version: "1.26.1"
#           cache-dependency-path: backend/go.sum
#       - uses: golangci/golangci-lint-action@v4
#         with:
#           version: latest
#           working-directory: backend
#           args: --timeout=5m

#   build:
#     needs: lint
#     runs-on: ubuntu-latest
#     permissions:
#       contents: read
#       packages: write
#       id-token: write
#       attestations: write
#     strategy:
#       matrix:
#         service: [backend, frontend]
#     steps:
#       - name: Install Cosign
#         uses: sigstore/cosign-installer@v3

#       - name: Checkout code
#         uses: actions/checkout@v4

#       - name: Log in to GHCR
#         uses: docker/login-action@v3
#         with:
#           registry: ghcr.io
#           username: ${{ github.actor }}
#           password: ${{ secrets.GITHUB_TOKEN }}

#       - name: Set up Docker Buildx
#         uses: docker/setup-buildx-action@v3

#       - name: Extract Metadata
#         id: meta
#         uses: docker/metadata-action@v5
#         with:
#           images: ghcr.io/${{ github.repository }}-${{ matrix.service }}
#           tags: |
#             type=sha,prefix=sha-
#             type=raw,value=latest

#       - name: Build locally
#         uses: docker/build-push-action@v6
#         with:
#           context: ./${{ matrix.service }}
#           load: true
#           push: false
#           tags: ${{ matrix.service }}:scan-candidate
#           cache-from: type=gha

#       - name: Scan with Trivy
#         uses: aquasecurity/trivy-action@master
#         with:
#           image-ref: ${{ matrix.service }}:scan-candidate
#           format: table
#           severity: CRITICAL,HIGH
#           exit-code: 1
#           ignore-unfixed: true

#       - name: Push
#         id: push
#         uses: docker/build-push-action@v6
#         with:
#           context: ./${{ matrix.service }}
#           push: true
#           tags: ${{ steps.meta.outputs.tags }}
#           labels: ${{ steps.meta.outputs.labels }}
#           cache-from: type=gha
#           cache-to: type=gha,mode=max
#           sbom: true
#           provenance: true

#       - name: Sign Image
#         run: |
#           cosign sign --yes \
#             ghcr.io/${{ github.repository }}-${{ matrix.service }}@${{ steps.push.outputs.digest }}
#         env:
#           COSIGN_EXPERIMENTAL: "1"

#   deploy-staging:
#     needs: build
#     if: github.ref == 'refs/heads/main'
#     uses: ./.github/workflows/deploy.yml
#     with:
#       environment: staging
#       image_tag: sha-${{ github.sha }}
#       app_url: http://staging.yourserver.com
#     secrets:
#       SSH_HOST: ${{ secrets.STAGING_SSH_HOST }}
#       SSH_USER: ${{ secrets.STAGING_SSH_USER }}
#       SSH_PRIVATE_KEY: ${{ secrets.STAGING_SSH_PRIVATE_KEY }}

#   deploy-production:
#     needs: deploy-staging
#     if: github.ref == 'refs/heads/main'
#     uses: ./.github/workflows/deploy.yml
#     with:
#       environment: production
#       image_tag: sha-${{ github.sha }}
#       app_url: http://yourserver.com
#     secrets:
#       SSH_HOST: ${{ secrets.PROD_SSH_HOST }}
#       SSH_USER: ${{ secrets.PROD_SSH_USER }}
#       SSH_PRIVATE_KEY: ${{ secrets.PROD_SSH_PRIVATE_KEY }}
